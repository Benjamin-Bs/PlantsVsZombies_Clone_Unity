using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class setPlantInformation : MonoBehaviour
{
    [SerializeField] 
    public Sprite icon;

    [SerializeField]
    public int cost;
    
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
